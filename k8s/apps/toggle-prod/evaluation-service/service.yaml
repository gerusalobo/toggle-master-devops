apiVersion: v1
kind: Service
metadata:
  name: evaluation-service
  namespace: toggle-prod

spec:
  type: ClusterIP

  selector:
    app: evaluation-service

  ports:
    - port: 8004
      targetPort: 8004
      protocol: TCP