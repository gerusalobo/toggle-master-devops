apiVersion: v1
kind: Pod
metadata:
  name: evaluation-bootstrap-test
  namespace: toggle-prod
spec:
  serviceAccountName: evaluation-api-key-bootstrap
  restartPolicy: Never

  containers:
    - name: test
      image: alpine:3.20
      command:
        - /bin/sh
        - -c
        - |
          apk add --no-cache aws-cli curl jq
          echo "Ambiente pronto."
          sleep 3600