apiVersion: apps/v1
kind: Deployment

metadata:
  name: analytics-service
  namespace: toggle-prod

spec:

  replicas: 1

  selector:
    matchLabels:
      app: analytics-service


  template:

    metadata:
      labels:
        app: analytics-service


    spec:

      containers:

        - name: analytics-service

          image: 784380251158.dkr.ecr.us-east-1.amazonaws.com/togglemaster/analytics-service:latest

          imagePullPolicy: Always


          ports:

            - containerPort: 8005


          envFrom:

            - configMapRef:
                name: analytics-config

            - secretRef:
                name: analytics-secret


          resources:

            requests:
              cpu: "50m"
              memory: "64Mi"

            limits:
              cpu: "250m"
              memory: "256Mi"



          readinessProbe:

            httpGet:
              path: /health
              port: 8005

            initialDelaySeconds: 10
            periodSeconds: 5



          livenessProbe:

            httpGet:
              path: /health
              port: 8005

            initialDelaySeconds: 30
            periodSeconds: 10